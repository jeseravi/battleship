import{Ship} from "./ship"

//this will be able to place ships at specific coordinates by calling the ship factory function

export const Gameboard = (xCoordinate,yCoordinate) => {

    //once this function is called, create a ship at the given coordinate
    


}